import React from 'react';

const TemporaryAlert = (props) => {
    return (
        <div id='temporary-alert'>
            <p>Your JSON was saved to the clipboard!</p>
        </div>
    );
};

export default TemporaryAlert;
